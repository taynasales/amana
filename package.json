{
  "name": "amana",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "prepare": "husky",
    "dev": "concurrently \"npm run dev --workspace=@amana/frontend\" \"npm run dev --workspace=@amana/backend\""
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "concurrently": "^10.0.4",
    "husky": "^9.1.7"
  }
}
